--- a/dlls/imm32/ime.c
+++ b/dlls/imm32/ime.c
@@ -464,6 +464,7 @@
     case WM_IME_CONTROL:
         FIXME( "hwnd %p, himc %p, msg %s, wparam %s, lparam %#Ix stub!\n", hwnd, himc,
                debugstr_wm_ime(msg), debugstr_imc(wparam), lparam );
+        if (wparam == IMC_CLOSESTATUSWINDOW) return 0;
         return 1;
     }
