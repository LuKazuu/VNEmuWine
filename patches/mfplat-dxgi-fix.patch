diff --git a/dlls/mfplat/main.c b/dlls/mfplat/main.c
index f0716cc..cfc831f 100644
--- a/dlls/mfplat/main.c
+++ b/dlls/mfplat/main.c
@@ -9690,6 +9690,15 @@ HRESULT WINAPI MFCreateDXGIDeviceManager(UINT *token, IMFDXGIDeviceManager **man
     if (!token || !manager)
         return E_POINTER;
 
+    {
+        const char *do_not_create = getenv("WINE_DO_NOT_CREATE_DXGI_DEVICE_MANAGER");
+        if (do_not_create && do_not_create[0] != '\0')
+        {
+            FIXME("WINE_DO_NOT_CREATE_DXGI_DEVICE_MANAGER set, stubbing out to fix Unity video freeze\n");
+            return E_NOTIMPL;
+        }
+    }
+
     if (!(object = calloc(1, sizeof(*object))))
         return E_OUTOFMEMORY;
 
